{
  "name": "@ondera/core",
  "version": "0.0.1",
  "private": true,
  "description": "Ondera command layer and session model. Pure TypeScript, no DOM, no Electron.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
